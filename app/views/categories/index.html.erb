<h2> Categories </h2>
<% @categories.each do |c| %>
  <ul class="listing">
    <div class="row">
      <div class="well col-md-4 col-md-offset-4">
        <li class="article-title">
          <%= link_to "#{c.name}", category_path(c) %></li>
      </div>
    </div>
  </ul>
<% end %>
